---
version: 2.0.0
title: client__src__generated__setup_ffi_mjs
---
// Generated. Do not edit.
//
// Browser startup bridge for multi-Mount root API apps.
// Derived from [[tools.rally.clients]] entries and the routes discovered
// for each Mount. It registers the Lustre effect transport, chooses the
// WebSocket URL from the current path, and sends page_init after connect
// so the server can create a RequestContext for this socket.
//
// The generator emits routePageInit from the same discovered routes as the
// client and server routers. Sign-in pages return null because they are
// SSR-only and should not open an authenticated WebSocket.
import { registerTransport } from "./runtime/client_effect_ffi.mjs";
import * as transport from "./transport_ffi.mjs";
import { decode_safe } from "./protocol_wire.mjs";
import { Ok } from "../../gleam_stdlib/gleam.mjs";
import { Some, None } from "../../gleam_stdlib/gleam/option.mjs";

export function setup() {
  registerTransport({
    sendToServer: (msg) => {
      const wsUrl = currentWsUrl();
      if (wsUrl) transport.send(wsUrl, "to_server", msg);
    },
    sendPageInit: (pageModule, pageParams, pageQuery) => {
      const wsUrl = currentWsUrl();
      if (wsUrl) transport.send_page_init(wsUrl, pageModule, pageParams, pageQuery);
    },
  });
  transport.registerOnConnect(() => {
    const wsUrl = currentWsUrl();
    const page = routePageInit();
    if (wsUrl && page) transport.send_page_init(wsUrl, page.module, page.params, page.query);
  });
  const wsUrl = currentWsUrl();
  if (wsUrl) transport.ensureSocket(wsUrl);
}

/** Read query params from the current browser URL. */
export function readCurrentQuery() {
  if (typeof window === "undefined") return {};
  const params = new URLSearchParams(window.location.search);
  const query = {};
  params.forEach((value, key) => { query[key] = value; });
  return query;
}

/** Resolve the browser window or a Node.js test globalThis mock. */
function ssrWindow() {
  return typeof window !== "undefined" ? window : globalThis.window;
}

/** Decode the SSR-embedded ToClient page-data payload (base64 ETF) into an Option(ToClient). */
export function readSsrToClient() {
  const win = ssrWindow();
  if (!win) return new None();
  const raw = win.__RUNTIME_SSR_TO_CLIENT__;
  if (!raw || raw === "" || raw === "{}") return new None();
  delete win.__RUNTIME_SSR_TO_CLIENT__;
  try {
    const binary = atob(raw);
    const bytes = new Uint8Array(binary.length);
    for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
    // TODO: Rust generator emits the SSR ToClient boot payload decoder here.
    const result = decode_safe(bytes);
    if (result instanceof Ok) {
      return new Some(result[0]);
    }
    return new None();
  } catch (_) { return new None(); }
}

/** Decode the SSR-embedded ClientSharedState payload (base64 ETF) into an Option(ClientSharedState). */
export function readClientSharedState() {
  const win = ssrWindow();
  if (!win) return new None();
  const raw = win.__RUNTIME_CLIENT_SHARED_STATE__;
  if (!raw || raw === "" || raw === "{}") return new None();
  delete win.__RUNTIME_CLIENT_SHARED_STATE__;
  try {
    const binary = atob(raw);
    const bytes = new Uint8Array(binary.length);
    for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
    // TODO: Rust generator emits the ClientSharedState boot payload decoder here.
    const result = decode_safe(bytes);
    if (result instanceof Ok) {
      return new Some(result[0]);
    }
    return new None();
  } catch (_) { return new None(); }
}

function currentWsUrl() {
  if (typeof window === "undefined") return "/ws";
  const path = window.location.pathname;
  if (path.startsWith("/sign_in")) return null;
  if (path.startsWith("/admin")) return "/admin/ws";
  return "/ws";
}

function routePageInit() {
  if (typeof window === "undefined") return { module: "Games", params: null, query: {} };

  const query = readCurrentQuery();
  const path = window.location.pathname.replace(/\/+$/, "") || "/";

  if (path === "/sign_in" || path.startsWith("/sign_in/")) {
    return null;
  }
  if (path === "/admin/games") return { module: "AdminGames", params: null, query };
  if (path === "/" || path === "/games") return { module: "Games", params: null, query };
  if (path === "/standings") return { module: "Standings", params: null, query };
  const gameMatch = path.match(/^\/games\/([^/]+)$/);
  if (gameMatch) return { module: "GamesId", params: decodeURIComponent(gameMatch[1]), query };
  const teamMatch = path.match(/^\/teams\/([^/]+)$/);
  if (teamMatch) return { module: "Team", params: decodeURIComponent(teamMatch[1]), query };
  // Unknown path: client router resolves to NotFound; skip page_init so
  // the WebSocket connection does not get a wrong request context.
  return null;
}

