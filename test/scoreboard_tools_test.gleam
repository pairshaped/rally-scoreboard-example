//// Scoreboard root package test entry point.
////
//// Gleeunit expects a package-level test module. The root package mostly hosts
//// Rally tooling config, so this test is intentionally small.

import gleeunit

pub fn main() {
  gleeunit.main()
}
