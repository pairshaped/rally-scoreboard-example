//// Root package marker for Scoreboard tooling.
////
//// This package lets the framework CLI run from the example root
//// while the app code stays split across client, server, and shared packages.

